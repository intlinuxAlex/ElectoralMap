const { BREAKPOINTS } = require('../constants/breakpoints');
const { colorsPalette } = require('../styled/colors');
const { mediaQueries } = require('../styled/mediaQuery');

module.exports = {
    BREAKPOINTS,
    colorsPalette,
    mediaQueries,
};
